include::_config.adoc[]

== generateSite

The xref:../../015_tasks/03_task_generateSite.adoc[`generateSite`] task creates a full documentation website.
It uses https://jbake.org[jBake] as a static site generator to produce a microsite with a landing page, navigation, local search, and edit links.

.Linux / macOS / WSL2
[role='primary']
--
[source, bash]
----
./dtcw4 local generateSite
----
--

.Windows with Powershell
[role='secondary']
--
[source, powershell]
----
.\dtcw.ps1 generateSite
----
--

The output is written to `build/microsite/output/`.
Open `build/microsite/output/index.html` in your browser to view the results.

NOTE: Some JavaScript-based features (like search) require serving the site via HTTP. Use any local web server, e.g. `python3 -m http.server -d build/microsite/output`.

:xrefstyle: short

[[html_out]]
.generated output of generateSite task
image::generateSite.png[]
